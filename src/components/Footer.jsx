import Section from './Section';

const Footer = () => {
    return (
        <Section crosses className="!px-0 !py-10">
            <div className="container flex items-center justify-center gap-10 max-sm:flex-col sm:justify-center">

            </div>
        </Section>
    )
};

export default Footer;
